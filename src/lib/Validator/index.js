/**
 * Validation function builder that takes in a list of rules and validations and returns a custom function
 * @param {Object} rules - specific options passed into validation functions. eg. allowed characters
 * @param {Array} validations - names of validations to run
 */
export default function Validator(rules = {}, validations = []) {
}
